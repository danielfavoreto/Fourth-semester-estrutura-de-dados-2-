#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>

void gera_ale (int n);
void gera_cre (int n);
void gera_dec (int n);
int cmpfunc_decrescente (const void * a, const void * b);
int cmpfunc_crescente (const void * a, const void * b);
